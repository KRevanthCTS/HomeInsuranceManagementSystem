spring.application.name=eureka-server

# The registry itself runs on the well-known Eureka port
server.port=8761

# This node IS the registry, so it should not try to register with itself
# or pull a registry from anywhere else.
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
eureka.instance.hostname=localhost
